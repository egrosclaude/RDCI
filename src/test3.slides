<!-- INCLUDE src/reveal.header -->

<section>
<span class="fragment" id="frag1"></span>
<div id="prueba" class="snapsvg" width="100%" height="100%">
<svg viewBox="0 0 600 400" width="100%" height="100%"></svg>
<!--
{
	"initialize" : "function(container) {
		var svg = Snap('#prueba svg'); 

		var g = svg.group();
		var r = svg.rect(20,20,100,50,2).attr({fill: '#6bb'});
		g.append(r);


		var zoomin1 = function() { svg.animate({viewBox: g.getBBox().vb},800,mina.easeinout); }; 
		var zoomout1 = function() { svg.animate({viewBox: '0 0 600 400'},800,mina.easeinout); };
		var shown = { 'frag1' : zoomin1, };
		var hidden = { 'frag1' : zoomout1, };
		Reveal.addEventListener('fragmentshown',function(event){ 
			if(typeof shown[event.fragment.id] === 'function') { shown[event.fragment.id](); } });
		Reveal.addEventListener('fragmenthidden',function(event){
			if(typeof hidden[event.fragment.id] === 'function') {hidden[event.fragment.id]()};});

	}"
}
-->
</div>
</section>

<!-- INCLUDE src/reveal.trailer -->
