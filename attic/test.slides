<!-- INCLUDE src/reveal.header -->
<!------------------------>
<section>
<h2>Snap test</h2>
<ul>
<li class="fragment">Veremos cómo interactuar</li>
<li class="fragment">entre fragmentos de texto y</li>
<li class="fragment">gráficos animados</li>
</ul>
<div id="myid" class="snapsvg" width="100%" height="100%">
	<svg viewBox="0 0 600 400" width="100%" height="100%"></svg>
	<!--
	{
		"initialize" : "function(container) {
			var svg = Snap('#myid svg');
			var r1 = svg.rect(20,20,50,60,5).attr({fill: '#BADA55'});
			var r2 = svg.rect(40,40,70,80,5).attr({fill: '#5ABAD0'});
			var r3 = svg.rect(80,40,20,30,5).attr({fill: '#BADDAD'});

			r1.drag(); r2.drag();


			var a = Snap.animation('opacity',1000,mina.linear);
			console.log(a);

			var fs = function(){ r1.transform('T190,60'); r3.transform('t500,30') };
			var fh = function(){ r1.transform('T50,40'); r3.transform('t-500,-30') };
			var fragmentsShown = { a };
			var fragmentsHidden = { pepe : fh };

			Reveal.addEventListener('fragmentshown', function(event){ fragmentsShown[event.fragment.id](); });
			Reveal.addEventListener('fragmenthidden', function(event){ fragmentsHidden[event.fragment.id](); });
	

		}"
	}
	-->
</div>
</section>

<section>
<h2>Snap test</h2>
<ul>
<li class="fragment">Veremos cómo interactuar</li>
<li class="fragment">entre fragmentos de texto y</li>
<li class="fragment">gráficos animados</li>
</ul>
<div id="myid" class="nada" width="100%" height="100%">
	<svg viewBox="0 0 600 400" width="100%" height="100%"></svg>
	<!--
	{
		"initialize" : "function(container) {
			var svg = Snap('#myid svg');
			var r1 = svg.rect(20,20,50,60,5).attr({fill: '#BADA55'});
			var r2 = svg.rect(40,40,70,80,5).attr({fill: '#5ABAD0'});
			var r3 = svg.rect(80,40,20,30,5).attr({fill: '#BADDAD'});

			r1.drag(); r2.drag();

			var a1 = Snap.animation('fill',2,mina.elastic);
			a1.attr({ id : 'a1' });
			a1.addClass('fragment');

			console.log(r2);

			var fragmentsShown = { a1 : fs };
			var fragmentsHidden = { a1 : fh };

			Reveal.addEventListener('fragmentshown', function(event){ fragmentsShown[event.fragment.id](); });
			Reveal.addEventListener('fragmenthidden', function(event){ fragmentsHidden[event.fragment.id](); });
	

		}"
	}
	-->
</div>
</section>

<!-----------------------------------------------------------------------------
		"fragments" : 
			{ rect1 : "rect1.attr({fill: '#DABABD'})" },
			{ rect2 : "rect1.transform('t150,50'); rect1.attr({fill: '#BADDAD'})" },
		]
------------------------------------------------------------------------------->
<!------------------------------------------------------------------------------->

<section data-transition="slide-in slide-out">
<h2>Prueba</h2>
<ul>
	<li>Probando</li>
	<ul>
		<li>Más pruebas</li>
	</ul>
</ul>
<!-- <img src="img/IMAGEN.png" class="plain stretch"> -->
<aside data-markdown class="notes">
## Prueba
* Vamos a ver si [Google](//www.google.com) funciona.
* Vamos a ver si este [link](https://en.wikipedia.org/wiki/Link-local_address) funciona.
* Vamos a ver si este [otro link](https://en.m.wikipedia.org/w/index.php?title=Link-local_address&mobileaction=toggle_view_mobile) funciona.
</aside>
</section>

<!-- INCLUDE src/reveal.trailer -->
